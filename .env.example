# JWT Secret for authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Application Environment
NODE_ENV=development

# Optional: Port configuration (Next.js uses 3000 by default)
PORT=3000